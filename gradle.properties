# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0
maven_group = moe.inx
archives_base_name = baby_zombie_remover

variant = simple

# Dependencies are managed at gradle/libs.versions.toml
